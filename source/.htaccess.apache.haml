- data.htaccess.errors.each do |code, document|
  ErrorDocument #{code} #{document}

RewriteEngine on
RewriteCond %{HTTP_HOST} ^makersacademy.se
RewriteRule (.*) https://craftacademy.se/$1 [R=301,L]

RewriteEngine on
RewriteCond %{HTTP_HOST} ^www.makersacademy.se
RewriteRule (.*) https://craftacademy.se/$1 [R=301,L]
